package ${ctx.daoPackage};

import java.util.List;
import ${ctx.modelPackage}.${ctx.modelClass};
import com.greatwall.health.framework.mapper.MybatisMapper;

/**
 * @desc ${ctx.tableCN}
 */
public interface ${ctx.daoClass} extends MybatisMapper<${ctx.modelClass}> {
	void addBatch(List<${ctx.modelClass}> ${ctx.modelObj}List);
}
